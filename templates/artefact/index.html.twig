{% extends 'base.html.twig' %}

{% block title %}Artefact List{% endblock %}

{% block pageheader %}
    <h1>Artefact List</h1>
    <p class="count">
        Displaying {{ artefacts|length }} artefacts of {{ artefacts.getTotalItemCount }} total.
    </p>
 {% endblock %} 

{% block body %}

    <div class='btn-toolbar pull-right'>
        <div class='btn-group'>
            {% if is_granted('ROLE_CONTENT_ADMIN') %}
            <a href="{{ path('artefact_new') }}" class="btn btn-default">
                <span class="glyphicon glyphicon-plus"></span> New
            </a>
            {% endif %}

            <a href="{{ path('artefact_search') }}" class="btn btn-default">
                <span class="glyphicon glyphicon-search"></span> Search
            </a>
        </div>
    </div>

    {% embed 'artefact/partial/table.html.twig' %}
    {% endembed %}

    <div class="navigation">
        {{ knp_pagination_render(artefacts) }}
    </div>

{% endblock %}
